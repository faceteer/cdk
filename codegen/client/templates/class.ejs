import { Gandalf } from '@tailwind/gandalf';
import { makeRequest } from './helpers/make-request';
<% for (const arg of functionNames) { -%>
import { <%= arg %> } from './requests/<%= arg %>';
<% } -%>

export type <%= serviceName %>Options = {
	gandalf: Gandalf;
	maxRetryAttempts?: number;
};

export class <%= serviceName %> {
	protected options: Required<<%= serviceName %>Options>;

	constructor({ gandalf, maxRetryAttempts = 4 }: <%= serviceName %>Options) {
		this.options = { gandalf, maxRetryAttempts };
	}

	<%_ for (const arg of functionNames) { -%>
	public <%= arg %> = <%= arg %>;
	<%_ } -%>

	protected makeRequest = makeRequest;
}
